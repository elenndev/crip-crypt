[package]
name = "safe-sync"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
age = { version = "0.11", features = ["armor"] }
secrecy = "0.8"

tar = "0.4"
flate2 = "1"

clap = { version = "4", features = ["derive"] }
chrono = "0.4"
